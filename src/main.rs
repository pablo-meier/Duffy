/* Mainline for Duffy . */

use std::logging;


fn main() {
  info!("Welcome to Beeper!");
  // parse command line options.

  // initiate the parse using the MIDI crate.

  // Use the parse MIDI struct to output the beep script for each of its tracks.
}
